# SML-1570 — Backfill regression tests for prior fixes

Test-only backfill from the post-0.8.19 review. Investigation found most fixes already shipped with regression tests; per scope decision, this PR fills only the genuinely-missing gaps (5 test-only edits, no source changes).

## Added regression coverage
| Item | File | Gap filled |
|------|------|-----------|
| SML-1518 | `packages/extension/src/commands/mcp-setup-cursor.test.ts` | EACCES (non-ENOENT) read error rethrows; no clobber/write |
| SML-1538 | `packages/extension/src/commands/registry-write.test.ts` | `toggleInlineDots` writes via `writeRegistry` (validation + `.bak` + cache-coherent) |
| SML-1543 | `packages/extension/src/appearance.test.ts` | absolute-path / Windows-backslash / dots-only styleId stay inside icon cache dir |
| SML-1542 | `packages/extension/src/ordering/rank.test.ts` | repeated same-gap reorders never collide; post-rebalance neighbors RANK_STEP-spaced & ordered |
| SML-1522 | `packages/server/src/tools/file-group.test.ts` | tagRemovals response asserts `agentActionRequired === true` |

## Already covered (no change)
- **SML-1519** — `bookmark-crud-multiroot.test.ts` already has dedicated multi-root `removeAtLine`/`clearFile`/`clearAll` tests.
- **SML-1531** — core `uri.test.ts` already pins relative/absolute `#L` and `#N` inputs with `not.toContain('%23')`. (Core repo; out of scope for this extension PR.)

Each added assertion is revert-sensitive: it fails if the underlying fix is reverted.
